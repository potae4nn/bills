import { BillMemberPipe } from './bill-member.pipe';

describe('BillMemberPipe', () => {
  it('create an instance', () => {
    const pipe = new BillMemberPipe();
    expect(pipe).toBeTruthy();
  });
});
